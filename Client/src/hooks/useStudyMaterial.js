/**
 * useStudyMaterial.js
 *
 * Convenience re-export of the StudyMaterialContext hook.
 *
 * Usage:
 *   import { useStudyMaterial } from '../hooks/useStudyMaterial';
 *   const { materials, loading, uploadMaterial, ... } = useStudyMaterial();
 */
export { useStudyMaterial } from '../context/StudyMaterialContext';
